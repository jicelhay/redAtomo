<%= form_for(@communication_post) do |f| %>
  <% if @communication_post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@communication_post.errors.count, "error") %> prohibited this communication_post from being saved:</h2>

      <ul>
      <% @communication_post.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h1> Configuración de <%= @school_class.name %>

<h2>Editar curso</h2>
<%= render 'form' %>

<h2>Administrar apoderados</h2>
<ul>
<% @school_class.parents.each do |parent|%>
    <li>
    <%=parent.name%>
    <%= link_to(fa_icon("trash-o"),parent_obligation_path(parent.id),method: :destroy)%>
    </li>
<%end%>
</ul>
 